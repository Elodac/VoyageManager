{
  "name": "voyagemanager",
  "version": "2.0.0",
  "description": "Application web de planification de voyages : destinations, carte, agenda, road trips, budget, valises et dossier imprimable.",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "serve": "npx --yes serve -l 8400 .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,css,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,json,md}\"",
    "check": "npm run lint && npm run format:check"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
